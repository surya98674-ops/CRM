import { Layout } from "../../components/layout/Layout.jsx";
import { LeadTable } from "../../components/shared/LeadTable.jsx";

const NotAnswered = () => {
  return (
    <Layout pageTitle="Not Answered">
      <LeadTable />
    </Layout>
  );
};

export default NotAnswered;
