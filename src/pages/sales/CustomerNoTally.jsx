import { Layout } from "../../components/layout/Layout.jsx";
import { LeadTable } from "../../components/shared/LeadTable.jsx";

const CustomerNoTally = () => {
  return (
    <Layout pageTitle="Customer Doesn't Have Tally">
      <LeadTable />
    </Layout>
  );
};

export default CustomerNoTally;
